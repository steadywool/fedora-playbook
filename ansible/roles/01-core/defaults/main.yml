---
common_packages:
  # Core
  - base
  - base-devel
  - "{{ kernel.name }}"
  - linux-firmware
  - pacman-contrib
  - "{{ kernel.microcode }}"

  # Filesystem
  - btrfs-progs
  - ntfs-3g

  # Compression
  - zip
  - unzip
  - unrar
  - p7zip

  # Editor
  - nano

  # Utils
  - git
  - wget
  - man-db
  - tree
  - ansible
  - rsync
