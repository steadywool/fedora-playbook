---
locale:
  lang: en_US.UTF-8
  keymap: fr

timezone: Europe/Paris
hostname: blue-swan

kernel:
  name: linux-hardened
  options: cryptdevice=/dev/sda2:luks-root root=/dev/mapper/luks-root rootflags=subvol=@ lsm=landlock,lockdown,yama,apparmor,bpf lockdown=integrity quiet rw
  microcode: intel-ucode
  hooks: base udev autodetect modconf kms keyboard keymap consolefont block encrypt filesystems fsck

aur_helper: yay-bin

user:
  name: kani
  password: kani # Change the password here
  groups: users,video,audio,games,adbusers,realtime
  shell: /bin/zsh

dotfiles: https://github.com/kaniville/linux-dotfiles.git

# vim: ft=yaml
