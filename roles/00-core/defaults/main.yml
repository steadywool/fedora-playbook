---
core_packages:
  # Base
  - base
  - base-devel
  - "{{ kernel.name }}"
  - "{{ kernel.name }}-headers"
  - linux-firmware
  - dkms
  - pacman-contrib
  - intel-ucode

  # Filesystem
  - btrfs-progs
  - ntfs-3g

  # Compression
  - zip
  - unzip
  - unrar
  - p7zip

  # Editor
  - nano

  # Utils
  - git
  - wget
  - man-db
  - ansible
  - tldr
  - rsync
  - jq
